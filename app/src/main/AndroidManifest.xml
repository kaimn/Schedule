<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schedule">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name="AppService"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.AddCourseActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activity.CourceScheduleActivity"
            android:theme="@style/AppThemeSchedule"/>
        <activity android:name=".activity.ChangeActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activity.OcrActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activity.LoginCourceActivity"
            android:label="@string/title_activity_coure_schedule"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>